# CPEN321_26W1_ProjectName

## Template Structure

| Path | Purpose |
| --- | --- |
| `.github/workflows/` | GitHub Actions workflows for CI/CD |
| `backend/` | Node.js + TypeScript backend |
| `documentation/` | Requirements, design, testing, and code review documents |
| `frontend/` | Android (Kotlin + Compose) frontend |
| `.eslintrc.json` | ESLint config read by Codacy (please do not move or modify) |
| `.gitignore` | Ignores build artifacts, secrets, and IDE files. Keep this file up to date so noise and sensitive data stay out of git |
| `detekt.yml` | Detekt config read by Codacy (please do not move or modify) |
| `docker-compose.yml` | Runs the backend and MongoDB via Docker Compose. Make sure to keep this file up to date |
| `scripts/` | Scripts TAs will use to deploy your frontend and backend code

## Frontend Setup 

<!-- Please specify the setup steps needed for frontend before running the script to build and run frontend --> 

## Backend Setup 

<!-- Please specify the setup steps needed for backend before running the script to build and run backend --> 

## Additional Setup 

<!-- Please specify any other additional setup steps non-specific to either frontend nor backend --> 

